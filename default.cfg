minarch_screen_scaling = Aspect
-overclock = off

bind Up = UP
bind Down = DOWN
bind Left = LEFT
bind Right = RIGHT
bind Select = SELECT
bind Start = START
bind A Button = A
bind B Button = B
bind X Button = X
bind Y Button = Y
bind L Button = L1
bind R Button = R1
bind L2 Button = L2
bind R2 Button = R2
